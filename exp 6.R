value <- 35

min_val <- 23
max_val <- 61

minmax <- (value - min_val) / (max_val - min_val)
minmax

mean_val <- 42
sd_val <- 12.94

zscore <- (value - mean_val) / sd_val
zscore

decimal_scaling <- value / 100
decimal_scaling
